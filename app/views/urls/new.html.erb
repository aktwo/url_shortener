<h1>Akshay's URL Shortener</h1>

<%= bootstrap_form_for @url, url: {action: "create"} do |f| %>
  <%= f.text_field :url, hide_label: true, placeholder: "http://www.example.com" %>
  <%= f.submit "Create", class: "btn btn-success" %>
<% end %>
